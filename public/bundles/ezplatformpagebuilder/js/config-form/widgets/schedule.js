(function(global, doc, eZ) {
    const { BlockConfigScheduleFieldBase } = eZ;

    const scheduleField = new BlockConfigScheduleFieldBase();

    scheduleField.init();
})(window, window.document, window.eZ);
